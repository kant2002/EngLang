@page "/statements"
<PageTitle>Statements</PageTitle>

<h1>Statements</h1>
<h2>Programming language conversion</h2>
<select @bind="Language" class="form-control">
    <option value="csharp">C#</option>
    <option value="js">JS</option>
</select>

<p>Statements used to perform calculation. There different kind of statements which express some formula, assignment statements and conditional statements</p>
<pre>
    @AssignmentStatementSample
</pre>
<p>which would be rougly translated to following code:</p>
<Compiler SourceCode="@AssignmentStatementSample" Language="@Language" />
<p>
    Same can be writen as:
</p>
<pre>put 10 into a value.</pre>
<p>You can assign variables to other variables.</p>
<pre>
    @AssignmentVariablesStatementSample
</pre>
<p>which as you may guess is this:</p>
<Compiler SourceCode="@AssignmentVariablesStatementSample" Language="@Language" />
<p>I have plans to support English script too</p>
<pre>
    @AssignmentStatementEnglishScriptSample
</pre>
<p>Right now it is:</p>
<Compiler SourceCode="@AssignmentStatementEnglishScriptSample" Language="@Language" />

<h2></h2>
<p>expression statements which can express math concepts </p>
<pre>
    @StatementSample
</pre>
<p>which would be rougly translated to following code:</p>
<Compiler SourceCode="@StatementSample" Language="@Language" />

<p>
    Statements separated by dot (.).
</p>
<p>which you would write as following:</p>
<pre>
    @MultipleStatementSample
</pre>
<Compiler SourceCode="@MultipleStatementSample" Language="@Language" />
<p>
    Same multiple statements can be expressed usign semicolon (;) as separator. You still need to end last sentence with dot (.).
</p>
<pre>
    @MultipleStatementUsingSemicolonsSample
</pre>
<Compiler SourceCode="@MultipleStatementUsingSemicolonsSample" Language="@Language" />
<p>
    Now to conditional statements. There only <b>if/then</b> variant supported. Right now you cannot express <b>else</b>.
</p>
<Compiler SourceCode="@IfStatementSample" Language="@Language" />
<p>
    For if statements, ability to separate statements using semicolon comes in handy.
</p>
<pre>
    @IfStatementMultipleThenSample
</pre>
<Compiler SourceCode="@IfStatementMultipleThenSample" Language="@Language" />
@code {
    private string? Language { get; set; } = "js";

    private string AssignmentStatementSample = @"let a value equals 10.";

    private string AssignmentVariablesStatementSample = @"put a value in a box.";

    private string AssignmentStatementEnglishScriptSample = @"let the initial value of a number be x.";

    private string StatementSample = @"add 42 to a value.";

    private string MultipleStatementSample = @"add 1 to a value. multiply a value by 3.";

    private string MultipleStatementUsingSemicolonsSample = @"add 1 to a value; multiply a value by 3.";

    private string IfStatementSample = @"if a number smaller than 0 then add 42 to a value.";

    private string IfStatementMultipleThenSample = @"if a number smaller than 0 then add 42 to a value; exit.";
}
